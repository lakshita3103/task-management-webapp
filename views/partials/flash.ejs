<% if (flash) { %>
  <div
    class="mt-6 rounded-2xl border px-4 py-3 text-sm font-medium shadow-sm <%= flash.type === 'success'
      ? 'border-emerald-200 bg-emerald-50 text-emerald-700'
      : 'border-rose-200 bg-rose-50 text-rose-700' %>"
  >
    <%= flash.message %>
  </div>
<% } %>
